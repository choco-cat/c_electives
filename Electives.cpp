#include "Prototypes and Classes.h"

// преписываю каждому факультативу своего преподавателя

FILE phisic, math, english, database, * programming;

//errno_t err;

/*int fileElectives()
{
    err = fopen_s(&phisic, "DataOfStudent.bin", "w+");
    if (!data)
    {
        printf("Ошибка при работе с файлом (его создании или открытии) .\n");
        return -2;
    }
    err = fopen_s(&data, "DataOfStudent.bin", "a+");
    if (!data)
    {
        printf("Ошибка при работе с файлом (его создании или открытии) .\n");
        return -2;
    }
    err = fopen_s(&data, "DataOfStudent.bin", "a+");
    if (!data)
    {
        printf("Ошибка при работе с файлом (его создании или открытии) .\n");
        return -2;
    }
    err = fopen_s(&data, "DataOfStudent.bin", "a+");
    if (!data)
    {
        printf("Ошибка при работе с файлом (его создании или открытии) .\n");
        return -2;
    }
    err = fopen_s(&data, "DataOfStudent.bin", "a+");
    if (!data)
    {
        printf("Ошибка при работе с файлом (его создании или открытии) .\n");
        return -2;
    }

    return 0;
}*/